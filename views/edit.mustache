<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1>Updating Old Data Yo</h1>

    <form method="POST" action="/edit/{{person_id}}">
      <input name="firstName" placeholder="first name" value="{{first_name}}" />
      <input name="lastName" placeholder="last name" value="{{last_name}}" />
      <input name="age" placeholder="age" value="{{age}}" />
      <button type="submit">save</button>
    </form>

  </body>
</html>
